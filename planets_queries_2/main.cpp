#include <bits/stdc++.h>

using namespace std;

/**
 * akanerva
 *
 * https://cses.fi/problemset/task/1160
 *
 * hups enpäs tiedäkkään nopeaa ratkaisua!
 *
 * ellei esim 1->2->3->4  -verkossa lasketa 2->4 väli kaavalla 1->4 - 1->2,
 * mutta sillekään en tiedä sopivaa tietorakennetta
 *
 */

const int maxN = 2e3+1;

int dist[maxN][maxN];
vector<int> tree[maxN];

int main() {

    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);



    return 0;
}